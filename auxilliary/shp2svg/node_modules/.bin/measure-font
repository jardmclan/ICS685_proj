#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-to-svg/bin/measure_font" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-to-svg/bin/measure_font" "$@"
  ret=$?
fi
exit $ret
